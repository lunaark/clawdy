# Clawdy 桌面宠物依赖
# 核心功能无需额外依赖（仅需 Python 3 自带的 tkinter）

# 可选：系统托盘图标（隐藏/显示 Clawdy）
pystray>=0.19
Pillow>=9.0

# 可选：全局快捷键（Ctrl+Shift+C 切换显示/隐藏）
pynput>=1.7

# 可选：Windows 音乐播放（macOS 用自带的 afplay，不需要）
# pygame>=2.5
